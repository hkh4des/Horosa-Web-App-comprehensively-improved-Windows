# Horosa 2.1.6 Beta

Release date / 发布日期: 2026-05-26

- Current release / 当前发布版本: `v2.1.6 Beta`
- GitHub Release visibility / GitHub 发布属性: normal release, `prerelease=false`
- Installer / 推荐安装包: `Horosa-Setup-2.1.6.exe`
- Platform / 平台: Windows 10/11 x64

## Summary / 概览

`v2.1.6 Beta` syncs two Mac-side fixes into Windows and batches in one Windows-only fix:
- **Qimen 奇门 calendar correction** (issue #4) — a low-level fix in the vendored Python engine `vendor/kinqimen`: month-pillar 月柱 now respects the precise 交节 (solar-term) crossing time, and 置闰 ju-determination uses the 超神接气置闰 solar term instead of an old lunar heuristic.
- **India chart map-pick** (issue #3) — `IndiaChartMain.js` `changeGeo` now sends a flat patch matching the parent `changeCond`.
- **Windows issue #2** (batched) — embedded Python/Java runtime isolated from a contaminated host environment (Win11 machines with system Python no longer fail to launch).

Pure Python (`vendor/kinqimen`) + frontend (`astrostudyui`) + Electron shell. **No Java change → `astrostudyboot.jar` was NOT rebuilt.**

`v2.1.6 Beta` 将 Mac 端的 **奇门历法底层修复** 与 **印度盘地图选点修复** 同步到 Windows，并合入本轮 Windows 侧 **issue #2 内置运行时环境隔离**。纯 Python（`vendor/kinqimen`）+ 前端 + Electron 外壳，**未改 Java，无需重建 `astrostudyboot.jar`**。

## What Changed / 本轮重点

- **奇门 `vendor/kinqimen/jieqi.py`**: `gangzhi` 月柱按 sxtwl 精确交节时刻校正（交节当日、交节前沿用前一日月柱；立春兼校年柱）；新增 `zhirun_jieqi`（超神接气置闰后的定局节气）。
- **奇门 `vendor/kinqimen/config.py`**: 重写 `qimen_ju_name_zhirun`（改用 `zhirun_jieqi` + `findyuen` + 既有局数表，弃用旧农历启发式）；新增 `dingju_jieqi`（拆补=历法节气 / 置闰=超神节气）。
- **奇门 `vendor/kinqimen/kinqimen.py`**: `pan()` 的「節氣」标签改用 `config.dingju_jieqi(...,option)`（置闰盘显示超神后的真实定局节气，拆补盘仍显示历法节气）。
- **奇门 `vendor/kinqimen/test_qimen_calendar.py`**: 新增 11 项回归测试（月柱边界 / 置闰 #62 / 拆补不变 / #43 / 闰大雪 / 整盘双法不崩）。
- **前端 `astrostudyui/src/components/astro/IndiaChartMain.js`**: `changeGeo` 改传扁平 `lon/lat/gpsLon/gpsLat`(+`tm`)， `AstroChartMain.changeGeo`（原把经纬度包成 `{value:...}` 与父级 `changeCond` 不符而报错）。
- **桌面 `desktop_installer_bundle/electron/service-manager.js`**（合并发布，Windows 侧 issue #2）: `sanitizeEmbeddedRuntimeEnv` 剥离宿主 `PYTHON*` / `_JAVA_OPTIONS|JAVA_TOOL_OPTIONS|JDK_JAVA_OPTIONS|CLASSPATH` 等污染变量；Python 以 `-E -s -X utf8` 启动；覆盖所有内置 python/java/`java -version`/jcmd 调用。

机制细节 / mechanism deep-dive: [`docs/qimen-calendar-fix-v2.1.6.md`](../qimen-calendar-fix-v2.1.6.md)。

## Files synced from Mac `main` (`494783d` / `v2.1.6`) / 同步文件

```
Horosa-Web/vendor/kinqimen/jieqi.py
Horosa-Web/vendor/kinqimen/config.py
Horosa-Web/vendor/kinqimen/kinqimen.py
Horosa-Web/vendor/kinqimen/test_qimen_calendar.py        (new)
Horosa-Web/astrostudyui/src/components/astro/IndiaChartMain.js
```

Confirmed against the precise Mac 2.1.5→2.1.6 commit-range diff (`8f3371f..494783d`) that these are the entire shared-tree change. All four modified files were byte-identical to the Mac-2.1.5 baseline on Windows (zero Windows divergence), so copying applied only the 2.1.6 change; the test file is a pure add. The Windows-only `service-manager.js` issue-#2 fix was committed earlier (`98bea3f`) and rides along in this release.

## Jar rebuild / 重建 jar

**Not required** — no `astrostudysrv/**` (Java) change this release. The qimen fix is in the vendored Python (`vendor/kinqimen`), which ships inside the runtime payload (staged by `stage:runtime` from the workspace).

## Carried forward / 沿用

- 2.1.5 (AI page hardening), 2.1.4 (AI provider compat + error surfacing + redaction), 2.1.3 (BaZi time), 2.1.2 (AI rework), 2.1.1 (clean-machine hardening).
- Windows-only fixes preserved: Feng Shui relative iframe path, window-jitter persistence guard, 策天飞星 brightness, `pages/index.js` field guards, `service-manager.js` desktop fast-path + embedded-runtime env isolation (issue #2).

## New dependencies / 新增依赖

- None.

## Windows Delivery / Windows 交付

Self-contained. Standard GitHub Release assets: `Horosa-Setup-2.1.6.exe`, `.exe.blockmap`, `latest.yml`, `SHA256SUMS.txt`.

## Verification / 验证

- Ported byte-exact from Mac `main`; Mac 2.1.5→2.1.6 commit-range diff = exactly these 5 shared-tree files (4 + 1 new test), 4 modified files at Mac-2.1.5 base with no Windows divergence.
- Qimen Python regression (`vendor/kinqimen/test_qimen_calendar.py`) run with the **embedded** interpreter: **11/11** (月柱 2005-05-05→庚辰 / 2021-05-05；交节后不回归；立春年柱+月柱；置闰 #62 立冬上元六局 + 标签；拆补不变；#43 拆补≠置闰；闰大雪；整盘双法不崩）。
- `astrostudyui` `npm run build:file`: compiled. `umi-test`: 28 suites / 129 tests passed (no regression).
- Issue #2 (batched): `service-manager.test.js` 18/18 (4 new env-isolation tests); real-`python.exe` integration under a poisoned env boots `isolated=1`.
- `npm run dist:win` (exit 0) + `release_selfcheck.py` gate at the END of `dist:win`: all checks (version consistency / sentinels incl. new qimen+India / staged-jar-not-stale / staged-dist-file-not-stale / release assets+hashes). Native-dependency gate.

## Local Asset State / 本地资产状态

- `latest.yml` version: `2.1.6`
- Installer size: `1194424731` bytes
- Blockmap size: `1234613` bytes
- Authenticode: per current release instruction (unsigned)

## SHA256

```text
b9bbe60651e8c535a0e1b6bf4c5f29c109cb1066ea1f93085dc8fdfced2a09d3  Horosa-Setup-2.1.6.exe
b71ab52bdade1b86e8bb9dca78e31b84f86713aa05982ebfa418d4d15d273935  Horosa-Setup-2.1.6.exe.blockmap
408679416c3d7c0a96f7dc65c5c02b6f3e92ecd35e914d9d0328bcea3ad590ac  latest.yml
```

## License & attribution / 许可与致谢

The qimen 置闰超神接气 ju-determination is a **classical public-domain algorithm**; the Python implementation is self-written, with structure/reference checks informed by public projects `Taogram/taobi` (MPL-2.0, VSOP87D precise, 拆补 cross-check), `redrockhorse/qimenpaipan`, and `qfdk/qimen` (MIT) — **no source files were copied**.
