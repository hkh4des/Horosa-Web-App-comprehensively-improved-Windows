# Horosa 2.1.4 Beta

Release date / 发布日期: 2026-05-26

- Current release / 当前发布版本: `v2.1.4 Beta`
- GitHub Release visibility / GitHub 发布属性: normal release, `prerelease=false`
- Installer / 推荐安装包: `Horosa-Setup-2.1.4.exe`
- Platform / 平台: Windows 10/11 x64

## Summary / 概览

`v2.1.4 Beta` syncs the latest Mac-side **AI Analysis backend-call fixes** into Windows, addressing reported GitHub issues that the AI backend call was broken with some providers/models. Three families: OpenAI reasoning-model compatibility, upstream error surfacing, and credential redaction. **Backend-heavy** — `astrostudyboot.jar` was rebuilt. Includes the 2.1.3 BaZi fix, the 2.1.2 AI rework, and the 2.1.1 clean-machine hardening.

`v2.1.4 Beta` 将 Mac 端最新的 **AI 分析后台调用修复** 同步到 Windows，解决 GitHub issues 反馈的「AI 后台调用有问题」。三类：OpenAI 推理模型兼容、上游错误透传、凭据脱敏。**以后端为主** —— 已重建 `astrostudyboot.jar`。

## What Changed / 本轮重点

- **A — OpenAI reasoning-model compatibility / 参数兼容** (`AIAnalysisProxyService.java`): new `isOpenAIReasoningModel` (matches `gpt-5.x`/`gpt-6`/`o1`/`o3`/`o4`, stripping any `provider/` prefix so `openai/gpt-5` works). `buildOpenAIChatBody` omits `temperature` and uses `max_completion_tokens` for these models; other models (e.g. `gpt-4.1`) unchanged.
- **B — Upstream error surfacing / 错误透传**: backend `ensureSuccess` reads the upstream error body (`readErrorBody`, truncated) and merges it into the `ErrorCodeException`; the frontend (`AIAnalysisMain.js`) handles a streamed `error` event and shows the real reason (`⚠️ <message>`) instead of the generic "模型未返回可用内容".
- **C — Credential redaction / 凭据脱敏** (`HttpUriRequestHystrixCommand.java`, shared `boundless` lib): `redactSensitiveHeaders` masks `Authorization`/`x-api-key`/`api-key`/`x-goog-api-key`/`cookie`/`proxy-authorization`/`localip` as `***redacted***` in error info; `stripQuery` removes `?key=` from logged URLs (e.g. Gemini keys).

## Files synced from Mac `main` (`65f2711`) / 同步文件

```
astrostudysrv/astrostudy/src/main/java/spacex/astrostudy/service/AIAnalysisProxyService.java   (A + B)
astrostudysrv/astrostudy/src/test/java/spacex/astrostudy/service/AIAnalysisProxyServiceTest.java (+ cases)
astrostudysrv/boundless/src/main/java/boundless/net/http/HttpUriRequestHystrixCommand.java       (C)
astrostudysrv/boundless/src/test/java/boundless/net/http/HttpUriRequestHystrixCommandTest.java    (new)
astrostudyui/src/components/aianalysis/AIAnalysisMain.js                                          (B, frontend)
```

Confirmed against the Mac 2.1.3→2.1.4 diff that these 5 files are the entire change (no Windows-ahead clobber; `AIAnalysisMain.js` change is additive over the 2.1.2 base).

## Jar rebuild (required — backend Java changed) / 重建 jar

`AIAnalysisProxyService` is in module `astrostudy`; `HttpUriRequestHystrixCommand` is in the shared `boundless` lib. No root reactor pom, fixed versions (boundless `1.2.1.2`, astrostudy/astrostudyboot `1.0.0`). Built with the bundled Maven + JDK 17:

```
mvn -o -f boundless/pom.xml  install -DskipTests
mvn -o -f astrostudy/pom.xml install -DskipTests
mvn -o -f astrostudyboot/pom.xml clean package -DskipTests   # clean required
```

Verified the fat jar contains the change: `BOOT-INF/lib/astrostudy-1.0.0.jar` → `AIAnalysisProxyService.class` has `max_completion_tokens`; `BOOT-INF/lib/boundless-1.2.1.2.jar` → `HttpUriRequestHystrixCommand.class` has `redacted`. Staged into `runtime/windows/bundle/astrostudyboot.jar` (the packaged jar re-verified to carry both).

## Carried forward / 沿用

- 2.1.3 BaZi time-display fix; 2.1.2 AI Analysis rework; 2.1.1 clean-machine hardening.
- Windows-only fixes preserved (not reverted): Feng Shui relative iframe path, window-jitter persistence guard, 策天飞星 brightness, `pages/index.js` field guards, `service-manager.js` desktop fast-path.

## New dependencies / 新增依赖

- None.

## Windows Delivery / Windows 交付

Self-contained. Standard GitHub Release assets: `Horosa-Setup-2.1.4.exe`, `Horosa-Setup-2.1.4.exe.blockmap`, `latest.yml`, `SHA256SUMS.txt`. Do not upload `win-unpacked`, QA logs, runtime caches, or reference source folders.

## Verification / 验证

- Ported byte-exact from Mac `main` (HEAD `65f2711`); Mac 2.1.3→2.1.4 diff = exactly these 5 files.
- JUnit (bundled Maven + JDK 17): `HttpUriRequestHystrixCommandTest` 2/2, `AIAnalysisProxyServiceTest` 14/14 — BUILD SUCCESS.
- `astrostudyboot.jar` rebuilt; both class markers verified inside the fat jar AND the staged bundle jar.
- `astrostudyui` `npm run build:file`: compiled successfully.
- `astrostudyui` `umi-test`: <!-- TODO fill -->
- `node --test electron/service-manager.test.js` + `release_preflight.py`: <!-- TODO fill -->
- `npm run dist:win`: passed (exit 0). `Horosa-Setup-2.1.4.exe` (`1194416512` bytes) + `.blockmap` + `latest.yml` (2.1.4) + `SHA256SUMS.txt`; native-dep gate 192 modules resolvable. The new `release_selfcheck.py` gate (now wired into `dist:win`) passed all 5 checks (version / sentinels / staged-jar-not-stale / staged-dist-file-not-stale / assets+hashes).
- Clean-machine cold/warm boot smoke: cold + warm both `backendReady` + `chartReady` (chart computed via the new jar), `forbiddenLogMatches` empty, ports freed — the new boundless+astrostudy jar boots the backend and serves cleanly. (Warm 10.018s marginally over the 10s soft threshold under concurrent build load; not a functional regression.)
- Release-script hardening tested: the full 8-module install chain builds offline and the resulting jar contains both markers.

## Local Asset State / 本地资产状态

- `latest.yml` version: `2.1.4`
- Installer size: `1194416512` bytes
- Blockmap size: `1239303` bytes
- Authenticode: per current release instruction

## SHA256

```text
84c6fb9ce94d6cf23f9b02d899ba0fc0b1a81e211817b56a5ba938a9189b487c  Horosa-Setup-2.1.4.exe
ec676ba79fbb356616466a0a34f2ff08c0c375568d6c131d5c8d2f62b7df5047  Horosa-Setup-2.1.4.exe.blockmap
87df91ad6591aa3e4e1b02d031adf60b7363b562d2b51ca7deea92190d362abe  latest.yml
```
