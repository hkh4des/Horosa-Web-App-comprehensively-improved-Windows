# Horosa 2.2.0 Beta

Release date / 发布日期: 2026-05-27

- Current release / 当前发布版本: `v2.2.0 Beta`
- GitHub Release visibility / GitHub 发布属性: normal release, `prerelease=false`
- Installer / 推荐安装包: `Horosa-Setup-2.2.0.exe`
- Platform / 平台: Windows 10/11 x64

## Summary / 概览

`v2.2.0 Beta` syncs a batch of Mac-side **feature** work — two new 数算 methods (邵子参评数 / 河洛理数), harmonic-chart (调波盘) rendering, a full **React rewrite of the Feng Shui 纳气盘**, and an AI model-selection fix — and **batches in a Windows-side fix for GitHub issue #7** (app couldn't launch: `spawn tar ENOENT`). Backend Java changed (AI model hardening), so `astrostudyboot.jar` was rebuilt.

`v2.2.0 Beta` 同步 Mac 端一批**功能更新**（数算 邵子参评数/河洛理数、调波盘绘制、风水纳气盘 React 重写、AI 模型选择修复），并合入 Windows 侧 **issue #7 修复**（`spawn tar ENOENT` 导致装好打不开）。含后端 Java 改动 → 重建 `astrostudyboot.jar`。

## What Changed / 本轮重点

**Mac-side (synced, Mac main `0ac85f0` / v2.2.0):**
- **数算 · 邵子参评数（canping）+ 河洛理数（heluo）** — new front-end-only techniques in the 数算 rail (`components/shusuan/{CanPingMain,HeLuoMain}.js`, `utils/{canpingLocal,heluoLocal}.js`, `utils/data/*.json`, wired in `components/kinastro/KinAstroMain.js`). Four pillars come from `utils/baziLunarLocal.js` — no kentang/Python backend.
- **调波盘绘制（harmonic）** — Python `astropy/astrostudy/{thirteenthchart,astroextra}.py` (`HarmonicChart` + `build_harmonic` returns a full chart); frontend `components/auxchart/{AstroHarmonicLab,AuxChartMain}.js`.
- **风水纳气盘 iframe → React** — new `components/fengshui/fengshuiEngine.js` (framework-agnostic canvas engine) + rewritten `components/fengshui/FengShuiMain.js` (Antd + `<canvas>`, ResizeObserver, AI snapshot); iframe deleted; `app.less` `:global{.horosa-fengshui-*}` layout + light/dark.
- **AI 模型选择修复** — `utils/aiAnalysisProviders.js` (Gemini `defaultChatModels`), `components/aianalysis/AIAnalysisMain.js` (`normalizeProfileChatModels`, test-connection uses the selected chat model), `utils/{aiAnalysisContext,aiExport}.js`; backend `AIAnalysisProxyService.java` defensively rejects an embedding model used as chat (`isEmbeddingModel` → 580014).

**Windows-side (batched in — GitHub issue #7):**
- **`spawn tar ENOENT` → app can't launch** (`service-manager.js`): payload extraction now resolves the OS tar to an absolute path (`%SystemRoot%\System32\tar.exe`) instead of a bare `tar` that relies on PATH/PATHEXT resolution (which ENOENT'd on the reporter's machine). New `resolveTarExe()` + actionable error.

机制细节 / mechanism: [`shusuan-canping-heluo-v2.1.9.md`](../shusuan-canping-heluo-v2.1.9.md), [`harmonic-chart-v2.1.9.md`](../harmonic-chart-v2.1.9.md), [`fengshui-react-rewrite-v2.1.9.md`](../fengshui-react-rewrite-v2.1.9.md), [`ai-model-selection-fix.md`](../ai-model-selection-fix.md). (Mac dev-named the first three `-v2.1.9`; they are this 2.2.0 release.)

## Files synced from Mac `main` (`56db820..0ac85f0`) / 同步文件

23 shared-tree files: 2 `astropy` Python (harmonic), 1 Java (`AIAnalysisProxyService`), 12 frontend modified + 8 new (shusuan pages/logic/data, fengshuiEngine, heluo build scripts). 21 were byte-identical to the Mac-2.1.8 baseline / pure adds (clean port); `FengShuiMain.js` was taken **wholesale** (the Windows-ahead iframe-path fix is **obsoleted** by the React rewrite — no iframe = no desktop file:// path issue); `app.less` was **3-way merged** (Mac-2.2.0 delta applied onto the Windows-ahead scroll tweak — clean, +273).

## Jar rebuild / 重建 jar

**Required (Java changed):** astrostudy `install` → astrostudyboot `clean package` (bundled Maven + JDK 17; boundless unchanged). Verified in the packaged fat jar: `AIAnalysisProxyService.class` contains `isEmbeddingModel`.

## Carried forward / 沿用

- 2.1.8 (Ollama #6 / bazi month-pillar / Taiyi / PD / **Tier-1 slim installer ~810 MB**), 2.1.7 (qimen true-solar-time), 2.1.6 (qimen calendar + India + issue #2 runtime isolation), 2.1.5…2.1.1.
- Windows-only fixes preserved: embedded-runtime env isolation, `app.less` scroll tweak (merged through), desktop fast-path, payload slimming. The Feng Shui iframe-path fix is intentionally superseded by the upstream React rewrite.

## New dependencies / 新增依赖

- None.

## Verification / 验证

- Precise diff `56db820..0ac85f0` = 23 shared-tree files; per-file classified (21 clean/add, FengShuiMain wholesale, app.less 3-way merged). FengShui confirmed React (0 iframe refs) post-port.
- Jar rebuilt; `isEmbeddingModel` verified in the packaged fat jar.
- issue #7: `resolveTarExe()` resolves `C:\Windows\System32\tar.exe` (verified `bsdtar` runs); `service-manager.test.js` 19/19 (new resolveTarExe test) — run via PowerShell.
- `astrostudyui` `npm run build:file` compiled; `umi-test` passed.
- `npm run dist:win` (exit 0) + `release_selfcheck.py` gate (version 2.2.0 / sentinels incl. updated FengShui `fengshuiEngine` + `isEmbeddingModel` + `resolveTarExe` / payload-slimmed / assets+hashes). Installer stays slim (~810 MB).

## Local Asset State / 本地资产状态

- `latest.yml` version: `2.2.0`
- Installer size: `849344035` bytes (≈810 MiB; Tier-1 slimming retained)
- Blockmap size: `880693` bytes
- Authenticode: per current release instruction (unsigned)

## SHA256

```text
223256365897f15a180a99ddde643f368e26fec1b102b4141d98c1e7c3915f49  Horosa-Setup-2.2.0.exe
1f1298dedbd643f37f3079202c306c6517a29da8ad7d7521d954b4a589a19fc4  Horosa-Setup-2.2.0.exe.blockmap
75dc9c3efba60ae28535434436ef7a5ed63f5f0a3fc0d1cc3ac586e16749eaef  latest.yml
```

## In-app verification / 应用内复核

数算→邵子参评数/河洛理数出盘；辅盘调波盘绘制；风水模块明暗主题 + 占满屏 + 纳气盘不裁；AI 分析 Gemini 等供应商「测试连接」用选中聊天模型、embedding 模型不被误用；issue #7 机器（System32 上有 tar.exe）应能正常启动。
