# 星阙 Horosa Windows 桌面版 `v1.0.4`

## 给谁下载

- **普通 Windows 10/11 x64 用户**：下载 `Horosa-Setup-1.0.4.exe`
- **如果你已经安装 `v1.0.3`**：建议升级到 `v1.0.4`
- **不要手动下载**：`latest.yml`、`Horosa-Setup-1.0.4.exe.blockmap`
  - 这两个文件给应用内自动更新使用，不是手动安装入口

## 三步安装

1. 下载 `Horosa-Setup-1.0.4.exe`
2. 双击安装器，按中文向导完成安装
3. 从桌面或开始菜单打开 `星阙`

## 离线说明

- 安装包已内置 Electron、Java、Python、前后端资源
- 安装和本地功能可离线运行
- 普通用户机器不需要预装开发环境、浏览器、Java 或 Python
- 只有“检查更新”与“下载更新”需要网络
- 默认安装到 `%LocalAppData%\Programs\Horosa`
- 用户数据保存在 `%LocalAppData%\HorosaDesktop`
- 升级、替换、修复默认保留用户数据

## 本次热修内容

### 1. 安装版默认显示修正

- 默认启动改为最大化窗口
- 默认界面缩放保持为 `0.8`
- 重启后继续保持“最大化 + 0.8”默认体验

### 2. 非 App 稳定版默认窗口修正

- `START_HERE.bat` 浏览器壳启动后默认最大化
- 页面内容缩放保持稳定版现状，不额外放大或缩小
- 持续兼容本地服务启动、浏览器 profile 与窗口纠正链路

### 3. 既有桌面修复继续保留

- 真正的 Electron 桌面窗口与完整离线安装包
- 中文维护页：已安装检测、替换 / 修复 / 取消
- 顶栏抽屉、易与三式、组合盘 / 时空中点盘等白屏修复
- 主 tab 底部越界修复
- 风水 tab 本地资源加载修复
- 安装版应用内更新与快捷方式稳定性修复

## 适用对象

- **普通用户**：直接下载 `.exe` 完整离线安装器
- **非 App 稳定版用户**：请下载 `windows-stable-2026-03-19` 对应的稳定版压缩包
- **开发者**：源码、脚本与桌面工程仍保留在 `main` 分支
